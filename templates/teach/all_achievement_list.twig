{# empty Twig template #}
<tr>
    <th class="th_title">选择</th>
    <th class="th_title">序号</th>
    <th class="th_title">项目编号</th>
    <th class="th_title">项目名称</th>
    <th class="th_title">姓名</th>
    <th class="th_title">身份证号</th>
    <th class="th_title">单位名称</th>
    <th class="th_title">联系方式</th>
    <th class="th_title">理论成绩</th>
    <th class="th_title">实操成绩</th>
    <th class="th_title">考试是否通过</th>
    <th class="th_title">录入日期</th>
    <th class="th_title">班主任</th>
    <th class="th_title">操作</th>
</tr>
{% for item in work_list %}
    <tr>
        <td><check type="checkbox"/></th>
<th>{{item.Id}}</th>
<th>{{item.ProCode}}</th>
<th>{{item.ProName}}</th>
<th>{{item.StudentName}}</th>
<th>{{item.StudentNum}}</th>
<th>{{item.UnitName}}</th>
<th>{{item.StudentPhone}}</th>
<th>{{item.TheoryAch}}</th>
<th>{{item.ActualOperatAch}}</th>
<th>{{item.IsAdopt}}</th>
<th>{{item.WriteDate}}</th>
<th>{{item.HeaderMaster}}</th>
<td>
    <input type="button" class="caozuo_1" onclick="toSubPage('../../modules/teach/setAchievement.php', '成绩管理', 2, 'modify', '{{item.Id}}', '{{pagetype}}')"   value="修改"/>
    <input type="button" class="caozuo_3"  onclick="deleteThis({{item.Id}})" value="删除"/></td>
</tr>
{% endfor %}



